export interface President {
  name: string;
  role: string;
  photo: string;
  bio: string;
  quote: string;
}

export interface BoardMember {
  name: string;
  role: string;
  photo: string;
  bio: string;
}

export interface FeaturedMember {
  name: string;
  role: string;
  accentColor: string;
  photo: string;
  bio: string;
}
